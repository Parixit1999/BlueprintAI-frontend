# Copy to .env.local to override. Defaults work for the standard dev loop.

# API base URL. Dev default: http://localhost:8000/api
# Production builds use VITE_API_BASE=/api (same-origin through CloudFront).
#VITE_API_BASE=http://localhost:8000/api
