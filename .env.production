# Production build config - used automatically by `npm run build`.
# The app is served from the same CloudFront domain as the API, so the
# API base is the relative /api path. No secrets belong in this file.
VITE_API_BASE=/api
